<div class="page-header">
	<h2><%= @project.facilityname %> Project</h2>
</div>

<div class="row">
	<div class="span6 offset3">
		<div class="well">
			<h4>Facility Name</h4><%= @project.facilityname %>
			<%= link_to 'View', @project.audit.url(:original) %>
		</div>
	</div>
</div>

<% if current_user == @project.user %>
	<%= link_to 'Edit', edit_project_path(@project) %> |
<% end %>
<%= link_to 'Back', projects_path %>
